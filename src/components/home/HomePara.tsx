import { styled } from 'styled-components';

const Container = styled.div`
  text-align: center;
  padding-bottom: 3rem;
`;

export default function HomePara() {
  return (
    <Container>
      안녕하세요.
    </Container>
  );
}
